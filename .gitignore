# Raw corpus / match data (2.2 GB) — regenerable, not version-controlled
matches/

# Large generated intermediates (exceed or strain GitHub limits) — regenerable from extract scripts
*.pkl
chunks_dense.tsv

# Editor backups / autosaves
\#*\#
*~
*.swp

# Python
__pycache__/
*.pyc
.venv/
venv/

# editions research intermediates (regenerable; final = sanskrit-editions.json)
editions_parts/
editions_workitems.json

tantric_parts/
tantric_workitems.json
